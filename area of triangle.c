#include<stdio.h>
main()
{
	int a,b,total;
	printf("Enter value of height :");
	scanf("%d",&a);
	printf("Enter value of width :");
	scanf("%d",&b);
	total=0.5*a*b;
	printf("Area of triangle is %d",total);
}
